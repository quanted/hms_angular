import { KeepScrollOnBottomDirective } from './keep-scroll-on-bottom.directive';

describe('KeepScrollOnBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepScrollOnBottomDirective();
    expect(directive).toBeTruthy();
  });
});
